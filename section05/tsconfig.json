{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
  "compilerOptions": {
    "baseUrl": ".", // 프로젝트 루트를 기준으로 경로 설정
    "paths": {
      "@/*": ["src/*"] // '@' 별칭을 'src'로 매핑
    }
  }
}
